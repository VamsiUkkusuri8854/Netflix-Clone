# .env.example
# Create a .env file and set these variables for deployment

# MongoDB Connection String (e.g. from MongoDB Atlas)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/netflix_clone

# JWT Secret for encoding tokens
JWT_SECRET=super-secret-production-key
